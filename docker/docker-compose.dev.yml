services:
  app:
    build:
      context: ..
      dockerfile: docker/dockerfile.dev

    image: fake-news-classifier:dev

    working_dir: /app

    volumes:
      - ..:/app

    command: sleep infinity